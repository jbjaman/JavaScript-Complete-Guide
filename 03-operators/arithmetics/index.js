// 1. Addition

const subTotal = 1000;
const shipping = 100;
const total98 = subTotal + shipping;
console.log(total98);

// 2. Subtraction

const balance = 5000;
const withdrawal = 1000;
const remaining = balance + withdrawal;
console.log(remaining);

// 3. Multiplication

const price = 500;
const quantity = 3;
const total = price * quantity;
console.log(total);

// 4. Division

const total99 = 1000;
const users = 4;
const perUser = total99 / users;

console.log(perUser);

// 5. Modulus %

const number = 10;

if (number % 2 === 0) {
  console.log("Even");
}

// 6. Exponentation

const exPonent = 2 ** 3;
console.log(exPonent);
